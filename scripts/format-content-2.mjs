/** Format guide content: executables, data & media. Prose may use <code>/<strong>/<em>/<a>. */
export const FORMAT_PAGES_2 = [
  {
    slug: 'elf',
    name: 'ELF',
    fullName: 'Executable and Linkable Format',
    group: 'executables',
    title: 'ELF file format — headers, segments and sections, explained',
    desc: 'The ELF format behind every Linux binary: the e_ident identification block, program headers (the loader view) versus section headers (the linker view), and how execution starts.',
    tagline: 'One format, two audiences: program headers tell the kernel what to map; section headers tell the linker what everything means. Neither needs the other.',
    oneLiner: 'Linux/Unix executables, libraries and .o files',
    demoId: 'elf',
    demoFile: 'demo.elf',
    magic: {
      hex: '7F 45 4C 46',
      ascii: '\\x7F E L F',
      note: 'The 16-byte <code>e_ident</code> block starts with <code>\\x7F</code> plus "ELF", then declares its own class (32/64-bit), endianness and ABI — the header describes how to read the rest of the header.',
    },
    intro: [
      'After <code>e_ident</code> comes the fixed header: type (<code>ET_EXEC</code> fixed-address, <code>ET_DYN</code> — which covers both shared libraries <em>and</em> modern PIE executables), machine, the <strong>entry point</strong> virtual address, and the offsets of two tables that partition the format&#39;s worldview.',
      'The <strong>program header table</strong> is the execution view: <code>PT_LOAD</code> segments the kernel maps into memory with read/write/execute flags, <code>PT_INTERP</code> naming the dynamic linker, <code>PT_DYNAMIC</code> for relocation data. The <strong>section header table</strong> is the linking view: <code>.text</code>, <code>.data</code>, <code>.bss</code>, symbol tables, string tables. Strip a binary and sections largely vanish — it still runs, because the loader never needed them.',
    ],
    structure: {
      caption: 'ELF64 header (after e_ident)',
      head: ['Field', 'Size', 'Meaning'],
      rows: [
        ['e_type', '2 B', '1 = relocatable .o, 2 = EXEC, 3 = DYN (libraries and PIE)'],
        ['e_machine', '2 B', '0x3E = x86-64, 0xB7 = AArch64, 0xF3 = RISC-V'],
        ['e_entry', '8 B', 'virtual address where execution starts'],
        ['e_phoff / e_shoff', '8+8 B', 'file offsets of the program / section header tables'],
        ['e_phnum / e_shnum', '2+2 B', 'how many entries each table has'],
      ],
    },
    quirks: [
      'A file can execute with zero sections — and be linkable with zero program headers (.o files). The two views are genuinely independent.',
      'Almost every distro binary today is ET_DYN: position-independent executables load at a random base for ASLR.',
      'The demo specimen here is a complete 170-byte static executable — header, one PT_LOAD, and a dozen instructions making write() and exit() syscalls.',
      'readelf -h and the hex inspector should always agree; when they don’t, someone hand-crafted the header (packers do).',
    ],
    faq: [
      {
        q: 'What is the difference between sections and segments in ELF?',
        a: 'Segments (program headers) are what the kernel maps at exec time — big regions with permissions. Sections (section headers) are what the linker and debugger care about — .text, .symtab, .debug_info. Sections are grouped inside segments, but only segments are needed to run, and only sections to link.',
      },
      {
        q: 'Why does my executable show type DYN instead of EXEC?',
        a: 'It is a position-independent executable (PIE): compiled to load at any base address so ASLR can randomize it. The kernel treats it like a shared object with an entry point. Non-PIE ET_EXEC binaries have become rare on modern distributions.',
      },
      {
        q: 'How small can an ELF executable be?',
        a: 'The header plus one loadable segment plus a few instructions — the specimen on this page is 170 bytes and runs on real x86-64 Linux. Golfed binaries overlap the program header with the ELF header to go smaller still; the record is well under 100 bytes.',
      },
    ],
    related: ['pe', 'mach-o', 'wasm'],
  },
  {
    slug: 'pe',
    name: 'PE',
    fullName: 'Portable Executable',
    group: 'executables',
    title: 'PE / EXE file format — MZ stub, headers and sections, explained',
    desc: 'Windows executables from the bytes up: the DOS MZ stub and e_lfanew pointer, COFF and Optional headers, RVAs, data directories, and the import table that links to DLLs.',
    tagline: 'Every Windows program still begins with a 1981 DOS program whose job is to say it cannot run — then a pointer skips 40 years forward.',
    oneLiner: 'Windows .exe/.dll: MZ stub → PE32+ headers',
    demoId: 'pe',
    demoFile: 'demo.exe',
    magic: {
      hex: '4D 5A',
      ascii: 'MZ',
      note: 'Mark Zbikowski&#39;s initials open a real DOS executable (the stub that prints "This program cannot be run in DOS mode"). The u32 at offset 0x3C — <code>e_lfanew</code> — points to the actual <code>PE\\0\\0</code> signature.',
    },
    intro: [
      'At <code>e_lfanew</code> sits <code>PE\\0\\0</code>, then the <strong>COFF header</strong> (machine, section count, characteristics) and the misnamed <strong>Optional header</strong> — mandatory for images — whose magic distinguishes PE32 from PE32+ (64-bit). It carries the entry point, image base, alignments, and the <strong>data directories</strong>: 16 (RVA, size) pairs locating imports, exports, relocations, resources, TLS, the certificate table…',
      'Then the <strong>section table</strong>: <code>.text</code>, <code>.rdata</code>, <code>.data</code>, <code>.rsrc</code>, each mapping a range of the file to a range of memory. Nearly every pointer in PE is an <strong>RVA</strong> — an offset from wherever the image loads — so parsing on disk means translating RVAs through the section table. It&#39;s the format&#39;s one real idea, and the one thing every new parser gets wrong first.',
    ],
    structure: {
      caption: 'The road from byte 0 to code',
      head: ['Part', 'Where', 'Contents'],
      rows: [
        ['MZ header + stub', '0', 'a real DOS program; e_lfanew at 0x3C points onward'],
        ['PE signature', 'e_lfanew', '"PE\\0\\0"'],
        ['COFF header', '+4', 'machine (0x8664 = x64, 0xAA64 = ARM64), NumberOfSections, timestamp'],
        ['Optional header', '+24', 'magic 0x20B = PE32+, AddressOfEntryPoint (RVA), ImageBase, subsystem'],
        ['Data directories', 'in Optional', '16 × (RVA, size): import, export, resource, certificate, …'],
        ['Section table', 'after headers', 'name, VirtualAddress, SizeOfRawData, PointerToRawData, flags'],
      ],
    },
    quirks: [
      'File offsets ≠ RVAs: sections are aligned to 512 B on disk and 4 KB in memory; every lookup goes through the section table.',
      'The Authenticode signature lives in a data directory pointed at by <em>file offset</em>, not RVA — the lone exception, and its bytes are excluded from the signed hash.',
      'The timestamp field is now often a reproducible-build hash, not a date — Microsoft broke the meaning deliberately.',
      'DLLs and EXEs are the same format; one Characteristics bit and the export table are the difference.',
    ],
    faq: [
      {
        q: 'Why do Windows executables contain a DOS program?',
        a: 'Backward compatibility from 1993: on real DOS the machine would execute the MZ stub, which prints "This program cannot be run in DOS mode" and exits. Modern Windows reads e_lfanew at offset 0x3C and jumps straight to the PE headers. The stub remains because removing it would break the file layout everyone parses.',
      },
      {
        q: 'What is an RVA?',
        a: 'A Relative Virtual Address: an offset from the image’s load base, valid only after sections are mapped. To find the bytes in the file you locate the section whose virtual range contains the RVA, then apply that section’s file-offset delta. Confusing RVAs with file offsets is the canonical PE parsing bug.',
      },
      {
        q: 'Where do I see which DLLs an EXE needs?',
        a: 'The import directory (data directory #1) lists each DLL by name with the functions imported from it. Drop an EXE into the hex inspector to walk the headers; dumpbin /imports or a PE viewer resolves the full table.',
      },
    ],
    related: ['elf', 'mach-o', 'java-class'],
  },
  {
    slug: 'mach-o',
    name: 'Mach-O',
    fullName: 'Mach Object',
    group: 'executables',
    title: 'Mach-O file format — load commands, segments, code signature, explained',
    desc: 'The macOS/iOS executable format: magic variants and universal binaries, the load-command list that drives the loader, segments like __TEXT and __LINKEDIT, and the embedded code signature.',
    tagline: 'A header and then a to-do list: load commands tell dyld every segment to map, dylib to load, and where main() is — the format is the instructions.',
    oneLiner: 'macOS/iOS binaries driven by load commands',
    demoId: 'macho',
    demoFile: 'demo.macho',
    magic: {
      hex: 'CF FA ED FE',
      ascii: 'feedfacf (LE)',
      note: '64-bit little-endian Mach-O. Its siblings: <code>CE FA ED FE</code> (32-bit), byte-swapped variants for big-endian history, and <code>CA FE BA BE</code> — the universal (fat) header that also collides with Java class files; disambiguated because fat arch counts are tiny numbers.',
    },
    intro: [
      'The header (magic, cputype, filetype, and <code>ncmds</code>/<code>sizeofcmds</code>) is followed by the format&#39;s engine: a list of <strong>load commands</strong>, each type + size + payload. <code>LC_SEGMENT_64</code> maps file ranges into memory (<code>__TEXT</code> holds code and the header itself, <code>__DATA</code> mutable state, <code>__LINKEDIT</code> the metadata heap), <code>LC_LOAD_DYLIB</code> names dependencies, <code>LC_MAIN</code> gives the entry offset, <code>LC_CODE_SIGNATURE</code> points at the signature blob.',
      'Universal ("fat") binaries prepend a big-endian table of (cputype, offset, size) entries, each pointing at a complete Mach-O — how one file ships x86_64 and arm64 side by side. On Apple Silicon every executable must be signed; ad-hoc signatures (a tree of SHA-256 page hashes, no identity) are inserted by the linker itself.',
    ],
    structure: {
      caption: 'Load commands you meet in every binary',
      head: ['Command', 'Role'],
      rows: [
        ['LC_SEGMENT_64', 'map a file range: __TEXT (r-x), __DATA_CONST, __DATA, __LINKEDIT'],
        ['LC_LOAD_DYLIB', 'a dependency by install-name, with version numbers'],
        ['LC_MAIN', 'entry point as an offset into __TEXT'],
        ['LC_UUID', '128-bit build UUID — how crash logs find the right dSYM'],
        ['LC_CODE_SIGNATURE', 'offset/size of the signature SuperBlob in __LINKEDIT'],
        ['LC_DYLD_CHAINED_FIXUPS', 'modern packed rebase/bind info (replaced classic dyld opcodes)'],
      ],
    },
    quirks: [
      'The Mach-O header lives <em>inside</em> the mapped __TEXT segment — the running process can read its own header at the image base.',
      'CA FE BA BE means fat Mach-O or Java class: parsers disambiguate by whether the next u32 is a plausible arch count (&lt; ~30) or a Java version (≥ 45).',
      '@rpath install names defer library resolution to LC_RPATH entries — the flexibility that powers app-bundled frameworks and half of macOS dylib-hijack findings.',
      'System dylibs aren’t on disk anymore: dyld resolves them from the shared cache blob (see the dyld_v1 magic in the inspector).',
    ],
    faq: [
      {
        q: 'How do universal (fat) binaries work?',
        a: 'A 12-byte-per-arch big-endian table at the start lists each architecture and the file offset of its complete Mach-O image. The kernel picks the best match and maps only that slice. lipo -info lists slices; the hex inspector shows the fat header and each embedded Mach-O.',
      },
      {
        q: 'Why does CAFEBABE mean both Java and Mach-O?',
        a: 'Coincidence from the same era: NeXT picked CAFEBABE for fat binaries, Sun picked it for class files. Parsers tell them apart by the following u32 — a fat binary has a small architecture count, while a class file has minor/major version where major ≥ 45 (Java 1.0).',
      },
      {
        q: 'What is an ad-hoc code signature?',
        a: 'A signature with page hashes but no signing identity: it proves the binary hasn’t changed since signing, not who signed it. On Apple Silicon the kernel refuses to run unsigned code, so the linker ad-hoc-signs every build automatically — visible as the LC_CODE_SIGNATURE blob in the demo specimen.',
      },
    ],
    related: ['elf', 'pe', 'java-class', 'dmg'],
  },
  {
    slug: 'java-class',
    name: 'Java class',
    fullName: 'JVM class file',
    group: 'executables',
    title: 'Java class file format — CAFEBABE and the constant pool, explained',
    desc: 'The JVM class file: the CAFEBABE magic, version numbers that gate loading, the constant pool everything points into, and how methods carry bytecode in Code attributes.',
    tagline: 'A big-endian format that is mostly one table: the constant pool, which every name, string, type and method reference indexes into.',
    oneLiner: 'JVM bytecode with everything in the constant pool',
    demoId: 'class',
    demoFile: 'Demo.class',
    magic: {
      hex: 'CA FE BA BE',
      ascii: '',
      note: 'Gosling&#39;s coffee-shop joke, chosen years before Java shipped. The next four bytes are minor/major version — major 52 = Java 8, 61 = Java 17, 65 = Java 21 — and a JVM refuses classes newer than itself.',
    },
    intro: [
      'After magic and version comes the <strong>constant pool</strong>: a 1-indexed table of tagged entries — UTF-8 strings, class references, field/method references, name-and-type pairs. Everything else in the file is u16 indexes into this pool; even the class&#39;s own name is "index of a Class entry pointing at a Utf8 entry".',
      'Then access flags, this/super class indexes, interfaces, fields, and methods. Each method&#39;s bytecode hides inside a <code>Code</code> <strong>attribute</strong> — the format&#39;s extension mechanism, also carrying line numbers, stack-map frames for verification, and annotations. The whole file is big-endian, u2/u4 all the way down.',
    ],
    structure: {
      caption: 'Constant pool entry tags (the common ones)',
      head: ['Tag', 'Type', 'Contents'],
      rows: [
        ['1', 'Utf8', 'length-prefixed modified UTF-8 bytes'],
        ['7', 'Class', 'u2 → Utf8 with the internal name (java/lang/String)'],
        ['8', 'String', 'u2 → Utf8 for a string literal'],
        ['9 / 10', 'Fieldref / Methodref', 'u2 class + u2 NameAndType'],
        ['12', 'NameAndType', 'u2 name + u2 descriptor like (I)V'],
        ['5 / 6', 'Long / Double', '8 bytes — and they consume TWO pool slots'],
      ],
    },
    quirks: [
      'Long and Double entries occupy two constant-pool indexes; the second is unusable — a 1996 decision the spec calls "a poor choice".',
      'Strings use <em>modified</em> UTF-8: NUL is encoded as C0 80, and supplementary characters as CESU-8 surrogate pairs.',
      'Method descriptors are a mini-language: <code>(ILjava/lang/String;)V</code> = takes int + String, returns void.',
      'The version bytes gate loading hard — "class file version 61 vs 52" is the byte-level reality behind "compiled by a newer JDK".',
    ],
    faq: [
      {
        q: 'How do I tell which Java version compiled a class file?',
        a: 'Bytes 6–7: the big-endian major version. 52 = Java 8, 55 = 11, 61 = 17, 65 = 21 (majors increment once per release). The hex inspector reads it out of the demo class; javap -v shows the same as "major version".',
      },
      {
        q: 'Why does everything reference the constant pool?',
        a: 'Deduplication and late binding: a method call site stores only a two-byte index to a Methodref, which chains to class-name and descriptor strings shared with every other use. It keeps files small and lets the JVM resolve symbolic references at link time — the file is a symbol graph, not machine code.',
      },
    ],
    related: ['wasm', 'mach-o', 'pe'],
  },
  {
    slug: 'wasm',
    name: 'WASM',
    fullName: 'WebAssembly binary',
    group: 'executables',
    title: 'WASM binary format — sections, LEB128 and the type system, explained',
    desc: 'The WebAssembly module format: the \\0asm magic, numbered sections from Type to Code, LEB128 variable-length integers everywhere, and how exports wire functions to the host.',
    tagline: 'A 2017 format with 1970s discipline: numbered sections in order, every integer LEB128, and a 41-byte module can export a working function.',
    oneLiner: 'portable bytecode modules for the web (and beyond)',
    demoId: 'wasm',
    demoFile: 'demo.wasm',
    magic: {
      hex: '00 61 73 6D 01 00 00 00',
      ascii: '\\0 a s m + version 1',
      note: 'The leading NUL is deliberate: it makes the file invalid as ASCII, HTML and every scripting language — nothing will ever execute a .wasm by accident.',
    },
    intro: [
      'A module is a sequence of <strong>sections</strong>, each: one id byte, a LEB128 size, contents. They must appear in id order — Type (1, function signatures), Import (2), Function (3, signature indexes), Memory (5), Export (7), Code (10, the actual bodies), Data (11). Custom sections (id 0, like <code>name</code> for debugging) may appear anywhere.',
      'Every integer is <strong>LEB128</strong> — the same varint as DWARF and protobuf: 7 bits per byte, high bit means "more". Function bodies declare their locals then give a stack-machine instruction stream (<code>local.get 0</code>, <code>i32.add</code>, <code>end</code>). The demo module on this page is 41 bytes and exports a callable <code>add(i32,i32)</code> — paste it into <code>WebAssembly.instantiate</code> and call it.',
    ],
    structure: {
      caption: 'Section ids',
      head: ['Id', 'Section', 'Contents'],
      rows: [
        ['0', 'Custom', 'named blobs — "name" carries function names for stack traces'],
        ['1', 'Type', 'function signatures: (params) → (results)'],
        ['3', 'Function', 'maps each function to a type index'],
        ['5', 'Memory', 'linear memory limits, in 64 KiB pages'],
        ['7', 'Export', 'name → function/memory/global/table index'],
        ['10', 'Code', 'per-function: locals + instruction stream'],
      ],
    },
    quirks: [
      'LEB128 allows over-long encodings (0x80 0x00 = 0) — validators must cap the byte count or 5-byte zeros lurk everywhere.',
      'Linear memory grows in 64 KiB pages and is bounds-checked; a module cannot address anything outside it — the sandbox is the address space.',
      'There are no mandatory function names in the binary; stack traces come from the optional name custom section, which is why release builds show wasm-function[7].',
      'The text format .wat is 1:1 with the binary — wasm2wat the demo module and read it like assembly.',
    ],
    faq: [
      {
        q: 'Why does WASM start with a NUL byte?',
        a: 'Defense by design: \\0asm cannot be a valid shell script, HTML page or ASCII text, so no interpreter will ever mistake a module for something executable-as-text. The four bytes after are the version, frozen at 1 since 2017 — features arrive via proposals, not version bumps.',
      },
      {
        q: 'Is WebAssembly only for browsers?',
        a: 'No — the same binary format runs in standalone runtimes (wasmtime, wasmer), edge platforms (Cloudflare Workers), and plugin systems. WASI standardizes system interfaces. The format itself is host-agnostic: sections, types and instructions say nothing about the web.',
      },
    ],
    related: ['elf', 'java-class', 'pe'],
  },
  {
    slug: 'binary-plist',
    name: 'binary plist',
    fullName: 'Apple binary property list',
    group: 'data',
    title: 'Binary plist format — bplist00, offset table and object graph, explained',
    desc: 'Inside Apple\'s binary property lists: the bplist00 magic, marker-nibble object encoding, the offset table, and the 32-byte trailer you must read first.',
    tagline: 'A serialized object graph read from the back: the trailer locates an offset table, the offsets locate objects, and the root is whichever one the trailer says.',
    oneLiner: 'Apple settings and metadata as a serialized graph',
    demoId: 'bplist',
    demoFile: 'demo.plist',
    magic: {
      hex: '62 70 6C 69 73 74 30 30',
      ascii: 'bplist00',
      note: 'Eight ASCII bytes: format + version. Parsing starts at the other end, though — the last 32 bytes are the trailer with the offset-table location, offset sizes, object count and root index.',
    },
    intro: [
      'Every object starts with a <strong>marker byte</strong>: high nibble = type (bool/int/real/date/data/string/array/dict), low nibble = size — or <code>0xF</code> meaning "a full int object with the real size follows". Strings come as ASCII or UTF-16BE, dates as seconds since 2001-01-01 (the Cocoa epoch), integers big-endian in power-of-two widths.',
      'Containers don&#39;t nest objects — they hold <strong>object references</strong>: indexes into the <strong>offset table</strong>, whose entries point at each object&#39;s file position. The trailer declares how wide offsets and references are (1–8 bytes each, scaled to file size). This indirection deduplicates shared values and encodes arbitrary graphs — the same object can appear in ten dictionaries while existing once.',
    ],
    structure: {
      caption: 'The 32-byte trailer (all big-endian)',
      head: ['Field', 'Size', 'Meaning'],
      rows: [
        ['(padding)', '6 B', 'unused'],
        ['offsetIntSize', '1 B', 'bytes per offset-table entry'],
        ['objectRefSize', '1 B', 'bytes per object reference inside containers'],
        ['numObjects', '8 B', 'total object count'],
        ['topObject', '8 B', 'index of the root object'],
        ['offsetTableOffset', '8 B', 'file position of the offset table'],
      ],
    },
    quirks: [
      'Dates are doubles counting from 2001-01-01 UTC — the Cocoa epoch, 978307200 seconds after Unix&#39;s.',
      'plutil -convert flips between binary and XML plists losslessly; the demo file is real plutil output.',
      'NSKeyedArchiver files are bplists <em>containing</em> another object graph ($objects array + UID references) — a graph serialized inside a graph serializer.',
      'iOS backups, Safari bookmarks, app defaults — all bplist; it may be the most-parsed format nobody has heard of.',
    ],
    faq: [
      {
        q: 'How do I read a binary plist without a Mac?',
        a: 'Drop it into the hex inspector — the parser walks the trailer, offset table and full object graph in your browser. Programmatically: python3’s plistlib reads both binary and XML plists on any OS.',
      },
      {
        q: 'Why does Apple use 2001 as the epoch?',
        a: 'NSDate counts from 2001-01-01 00:00:00 UTC — the "Cocoa epoch", chosen as a round recent date when Mac OS X shipped. Binary plists inherit it for date objects: read the double, add 978307200, and you have Unix time. The timestamp converter on this site does it for you.',
      },
    ],
    related: ['appledouble', 'dmg', 'icns', 'sqlite'],
  },
  {
    slug: 'appledouble',
    name: 'AppleDouble',
    fullName: 'AppleSingle / AppleDouble',
    group: 'data',
    title: 'AppleDouble format — the ._ files on your USB stick, explained',
    desc: 'What the hidden ._ files are: AppleDouble headers storing Finder info and extended attributes for filesystems without native support, entry by entry.',
    tagline: 'The answer to "what are these ._ files?": macOS metadata exiled to a sidecar when the filesystem can&#39;t hold it natively.',
    oneLiner: 'the hidden ._ metadata sidecar files',
    demoId: 'appledouble',
    demoFile: 'apple-double.bin',
    magic: {
      hex: '00 05 16 07',
      ascii: '',
      note: 'AppleDouble&#39;s magic; its sibling AppleSingle uses <code>00 05 16 00</code>. Double = metadata in a separate <code>._name</code> file next to the data; Single = both forks in one file.',
    },
    intro: [
      'Classic Mac files had two forks — data and resource — plus Finder metadata. Copy that onto FAT32 or a network share and there&#39;s nowhere to put the extra parts, so macOS writes an <strong>AppleDouble sidecar</strong>: <code>._photo.jpg</code> next to <code>photo.jpg</code>.',
      'The layout is a big-endian header: magic, version, then a count of <strong>entries</strong>, each (entry id, offset, length). Entry 9 is the resource fork, entry 2 Finder info; modern macOS packs <strong>extended attributes</strong> (quarantine flags, tags, provenance) into an ATTR blob inside the Finder-info entry. The demo file is genuine <code>ditto</code> output with real xattrs to walk.',
    ],
    structure: {
      caption: 'Entry ids',
      head: ['Id', 'Entry', 'Contents'],
      rows: [
        ['1', 'Data fork', 'AppleSingle only — the actual file contents'],
        ['2', 'Resource fork', 'classic Mac structured resources'],
        ['9', 'Finder info', '32 bytes type/creator/flags + modern ATTR xattr blob appended'],
        ['8', 'File dates', 'creation/modification as seconds from 2000-01-01'],
        ['3 / 11', 'Real name / Directory ID', 'rarely present'],
      ],
    },
    quirks: [
      'The ubiquitous <code>__MACOSX/</code> folder in ZIPs is AppleDouble files zipped along as metadata — harmless, deletable, eternally confusing.',
      'dot_clean on macOS merges sidecars back into native xattrs; deleting ._ files loses tags, quarantine state and resource forks silently.',
      'The xattr blob has its own little format (ATTR magic, offset table) nested inside entry 2 — a format inside a format.',
      'File dates count from 2000-01-01 — yet another epoch (the timestamp converter knows it).',
    ],
    faq: [
      {
        q: 'Can I safely delete ._ files?',
        a: 'If you don’t care about Mac-specific metadata (Finder tags, quarantine provenance, resource forks), yes — the main file is untouched. They regenerate whenever macOS writes to a filesystem without native xattr support. On the Mac side, dot_clean folds them back in.',
      },
      {
        q: 'Why does macOS keep creating them on my USB drive?',
        a: 'The drive is FAT32/exFAT, which has no extended-attribute support. Every xattr write (and Finder metadata) must go somewhere, so macOS mirrors it into the AppleDouble sidecar. Format the drive APFS/HFS+ (Mac-only use) and they disappear.',
      },
    ],
    related: ['binary-plist', 'dmg', 'zip'],
  },
  {
    slug: 'sqlite',
    name: 'SQLite',
    fullName: 'SQLite database',
    group: 'data',
    title: 'SQLite file format — pages, B-trees and varints, explained',
    desc: 'The SQLite database file: the 100-byte header, fixed-size pages, table and index B-trees, record serial types, and why the format is a US Library of Congress recommended storage format.',
    tagline: 'A single file honest enough to document every byte: 100 bytes of header, then nothing but fixed-size pages holding B-trees.',
    oneLiner: 'the world&#39;s most deployed database, one file',
    demoId: 'sqlite',
    demoFile: 'demo.sqlite',
    magic: {
      hex: '53 51 4C 69 74 65 20 66 6F 72 6D 61 74 20 33 00',
      ascii: 'SQLite format 3\\0',
      note: 'Sixteen bytes of self-description ending in NUL. The rest of the 100-byte header: page size, text encoding, schema version, page count — all big-endian.',
    },
    intro: [
      'After the header the file is an array of <strong>pages</strong> (512–65536 bytes, power of two; page 1 contains the header itself). Each page is a <strong>B-tree node</strong>: table B-trees keyed by rowid hold your rows in leaf cells; index B-trees hold key tuples. Page 1&#39;s B-tree is <code>sqlite_schema</code> — the table of tables, whose rows contain the <code>CREATE TABLE</code> text and each table&#39;s root page number.',
      'Row data uses <strong>records</strong>: a header of varint <em>serial types</em> (0 = NULL, 1–6 = ints of growing width, 7 = float, ≥13 odd = text of length (n−13)/2, ≥12 even = blob) followed by the values. Varints are big-endian 7-bit chunks, at most 9 bytes. Overflowing cells chain onto overflow pages; freed pages go on a freelist for reuse.',
    ],
    structure: {
      caption: 'The 100-byte header (selected fields, big-endian)',
      head: ['Offset', 'Field', 'Meaning'],
      rows: [
        ['16', 'page size', 'u16; 1 means 65536'],
        ['18 / 19', 'write / read version', '1 = rollback journal, 2 = WAL mode'],
        ['28', 'page count', 'file size in pages'],
        ['32', 'freelist head', 'first reusable page (deleted data can linger here)'],
        ['44', 'schema cookie', 'bumps on every DDL change'],
        ['56', 'text encoding', '1 = UTF-8, 2/3 = UTF-16 LE/BE'],
      ],
    },
    quirks: [
      'DELETE doesn&#39;t erase: freed pages and cells keep old bytes until VACUUM rewrites the file — the basis of SQLite forensics.',
      'A row&#39;s INTEGER PRIMARY KEY <em>is</em> the rowid; the column stores nothing at all.',
      'In WAL mode recent commits live in a separate -wal file — copy only the .sqlite and you may miss the newest data.',
      'The format is so stable the US Library of Congress lists it as a recommended archival storage format alongside CSV and XML.',
    ],
    faq: [
      {
        q: 'Can I recover deleted rows from an SQLite database?',
        a: 'Often, partially: deletion moves pages/cells to freelists without wiping them, so old values linger until VACUUM or new writes reuse the space. Forensics tools walk freelist pages and unallocated cell space. If secure_delete is enabled, SQLite zeroes instead — and nothing lingers.',
      },
      {
        q: 'Why does my app’s data survive in a single file?',
        a: 'SQLite implements the whole database — schema, tables, indexes, transactions — inside one file using B-trees over fixed pages, with a journal or WAL sidecar during writes for crash safety. That one-file property is why browsers, phones and messaging apps embed it: state is a file copy away.',
      },
    ],
    related: ['binary-plist', 'pdf', 'zip'],
  },
  {
    slug: 'pdf',
    name: 'PDF',
    fullName: 'Portable Document Format',
    group: 'data',
    title: 'PDF file format — objects, xref table and trailer, explained',
    desc: 'How PDF works underneath: numbered objects, the cross-reference table with its 20-byte entries, the trailer read from the end, incremental updates, and content streams.',
    tagline: 'A graph of numbered objects with a lookup table at the end — append-only updates mean old versions of a "changed" PDF are often still inside.',
    oneLiner: 'documents as an object graph with an end-of-file index',
    demoId: 'pdf',
    demoFile: 'demo.pdf',
    magic: {
      hex: '25 50 44 46 2D',
      ascii: '%PDF-',
      note: 'An ASCII comment: <code>%PDF-1.7</code>. Line two is traditionally four bytes ≥ 0x80 — garbage on purpose, so transfer tools treat the file as binary. Reading starts at the other end, at <code>startxref</code>.',
    },
    intro: [
      'A PDF is a bag of <strong>indirect objects</strong> — <code>3 0 obj … endobj</code> — that reference each other by number (<code>3 0 R</code>). Pages, fonts, images and content streams are all objects; the <strong>trailer</strong> names the root (Catalog), and the <strong>xref table</strong> maps every object number to its byte offset with fussy 20-byte fixed-width entries.',
      'A reader jumps to the end, reads <code>startxref</code> for the table&#39;s position, then resolves objects lazily. Editing appends: new objects, a new xref section, a new trailer chained to the old — <strong>incremental update</strong>. The original bytes stay in the file, which has leaked redacted text more than once. Since PDF 1.5, xref itself can live in a compressed stream object, and objects can pack into object streams.',
    ],
    structure: {
      caption: 'Object graph, minimally',
      head: ['Object', 'Role'],
      rows: [
        ['Catalog', 'the root: points at the page tree'],
        ['Pages / Page', 'the tree of pages; each Page has MediaBox, Resources, Contents'],
        ['Content stream', 'drawing operators: "BT /F1 24 Tf 100 700 Td (Hello) Tj ET"'],
        ['Font / XObject', 'resources referenced by name from content streams'],
        ['xref + trailer', 'offset table + root pointer + previous-xref chain'],
      ],
    },
    quirks: [
      'Offsets in xref must be byte-exact — one wrong digit and objects vanish; the demo file&#39;s table is correct and worth studying in the inspector.',
      'Incremental updates mean "deleted" content often remains: parse older trailers (/Prev chain) to see document history.',
      'Streams declare /Length and usually /Filter (FlateDecode = zlib) — text extraction fights compression, not the format.',
      'The spec allows a PDF to start anywhere in the first 1024 bytes — polyglot files exploit that generosity.',
    ],
    faq: [
      {
        q: 'Why can "deleted" text still be inside a PDF?',
        a: 'Because saves are often incremental: edits append new objects and a new xref rather than rewriting the file. The superseded objects — including "redacted" text drawn over with black boxes — remain reachable through the previous trailer chain. Proper redaction requires rewriting the document, not annotating it.',
      },
      {
        q: 'Why do PDF tools complain about a broken xref table?',
        a: 'The xref entries are fixed-width (exactly 20 bytes, "0000000017 00000 n\\r\\n") and hold absolute byte offsets. Anything that shifts bytes — text-mode transfer, naive concatenation, hand editing — invalidates every offset. Robust readers rebuild the table by scanning for "obj" markers; strict ones refuse.',
      },
    ],
    related: ['sqlite', 'zip', 'jpeg'],
  },
  {
    slug: 'wav',
    name: 'WAV',
    fullName: 'RIFF / WAVE audio',
    group: 'media',
    title: 'WAV file format — RIFF chunks and the fmt contract, explained',
    desc: 'The WAV format: RIFF container with FourCC chunks, the fmt chunk as the decoder contract (rate, channels, bit depth), and raw little-endian PCM samples in data.',
    tagline: 'A 1991 Microsoft/IBM container where the fmt chunk is a contract: sample rate, channels, bit depth — and then data is just the samples, raw.',
    oneLiner: 'chunked container, usually raw PCM audio',
    demoId: 'wav',
    demoFile: 'demo.wav',
    magic: {
      hex: '52 49 46 46 ·· ·· ·· ·· 57 41 56 45',
      ascii: 'RIFF….WAVE',
      note: '<code>RIFF</code> + little-endian file size − 8, then the form type <code>WAVE</code>. RIFF is the generic container; AVI and old ANI cursors use the same envelope with different form types.',
    },
    intro: [
      'RIFF is chunks: a FourCC id, a little-endian u32 size, data, and a pad byte when the size is odd. WAVE requires <code>fmt&nbsp;</code> (note the space — FourCCs are always 4 bytes) before <code>data</code>. The fmt chunk is the entire decoder contract: format tag (1 = PCM, 3 = float, 0xFFFE = extensible), channels, sample rate, byte rate, block align, bits per sample.',
      'Then <code>data</code> holds samples: little-endian, channels interleaved per frame (left, right, left, right…), 8-bit unsigned but 16/24/32-bit signed — an asymmetry inherited from 1991. The demo file is a playable 440 Hz beep; change the sample-rate field in the hex inspector, download, and hear the pitch shift.',
    ],
    structure: {
      caption: 'fmt chunk (PCM)',
      head: ['Field', 'Size', 'Meaning'],
      rows: [
        ['wFormatTag', '2 B', '1 = integer PCM, 3 = IEEE float, 0xFFFE = WAVE_FORMAT_EXTENSIBLE'],
        ['nChannels', '2 B', '1 = mono, 2 = stereo, …'],
        ['nSamplesPerSec', '4 B', 'sample rate: 44100, 48000, …'],
        ['nAvgBytesPerSec', '4 B', 'rate × block align — players use it for duration estimates'],
        ['nBlockAlign', '2 B', 'bytes per frame: channels × bits/8'],
        ['wBitsPerSample', '2 B', '8 (unsigned) / 16 / 24 / 32 (signed)'],
      ],
    },
    quirks: [
      'u32 sizes cap WAV at 4 GB — long multitrack recordings overflow into RF64/BW64, which redirect sizes to a ds64 chunk.',
      '8-bit samples are unsigned (silence = 0x80), 16-bit are signed (silence = 0x0000) — mixing them up produces loud distortion.',
      'Extra chunks are common and skippable: LIST/INFO metadata, cue points, broadcast bext — parsers must walk chunk-to-chunk, not assume data follows fmt.',
      'An odd-sized chunk gets a pad byte that its size field does not include — the classic RIFF parsing bug.',
    ],
    faq: [
      {
        q: 'Is WAV lossless?',
        a: 'WAV is a container; what’s inside decides. The overwhelmingly common payload is uncompressed PCM — bit-exact, lossless, large (~10 MB/min for CD stereo). The format tag can also declare float, ADPCM or even MP3 payloads, but in practice WAV means raw PCM.',
      },
      {
        q: 'Where is a WAV file’s duration stored?',
        a: 'Nowhere — it’s computed: data-chunk size ÷ byte rate (nAvgBytesPerSec). Change the sample rate field and the same data plays slower or faster; the hex inspector demo makes that a 10-second experiment.',
      },
    ],
    related: ['mp4', 'bmp', 'gif'],
  },
  {
    slug: 'mp4',
    name: 'MP4',
    fullName: 'ISO Base Media File Format',
    group: 'media',
    title: 'MP4 file format — boxes, ftyp brands and the moov/mdat split, explained',
    desc: 'MP4/M4A under the hood: size-prefixed boxes with FourCC types, ftyp compatibility brands, the moov metadata tree versus mdat sample data, and why "fast start" moves moov first.',
    tagline: 'Everything is a box: 4-byte size, 4-byte type, contents — nested into a metadata tree (moov) that indexes a big bag of samples (mdat).',
    oneLiner: 'video/audio in nested boxes (ISO BMFF)',
    demoId: 'm4a',
    demoFile: 'demo.m4a',
    magic: {
      hex: '·· ·· ·· ·· 66 74 79 70',
      ascii: 'size + ftyp',
      note: 'MP4 has no offset-0 magic: the first box is <code>ftyp</code>, so its type appears at offset 4 after the box size. ftyp&#39;s payload lists <em>brands</em> — <code>isom</code>, <code>M4A&nbsp;</code>, <code>mp42</code> — declaring which specs the file claims compatibility with.',
    },
    intro: [
      'Every structure is a <strong>box</strong> (the spec&#39;s word for atom): u32 big-endian size, FourCC type, payload — where size 1 switches to a u64 largesize for >4 GB boxes. Boxes nest: <code>moov</code> contains <code>trak</code> per track, each with media info and the four sample tables that index everything: <code>stts</code> (timing), <code>stsc</code> (chunking), <code>stsz</code> (sizes), <code>stco</code>/<code>co64</code> (chunk offsets).',
      'The actual audio/video frames sit in <code>mdat</code> as one opaque blob — meaningless without moov&#39;s tables. That split is the format&#39;s big idea and its operational quirk: if moov comes after mdat (the default for many encoders), a streaming player must download the whole file before playing. "Fast start" remuxes moov to the front. The demo is a real AAC file from afconvert; its box tree reads like a table of contents.',
    ],
    structure: {
      caption: 'The boxes that matter',
      head: ['Box', 'Contents'],
      rows: [
        ['ftyp', 'major brand + compatible brands — what the file claims to be'],
        ['moov', 'the metadata tree: movie header, one trak per stream'],
        ['moov/trak/mdia/minf/stbl', 'the sample tables: stts, stsc, stsz, stco — the index into mdat'],
        ['mdat', 'the samples themselves, raw and unframed'],
        ['moof', 'fragmented MP4: movie fragments for streaming (DASH/HLS)'],
        ['udta / meta / ilst', 'metadata; iTunes tags use OSType keys like ©nam'],
      ],
    },
    quirks: [
      'stco chunk offsets are absolute file positions — moving moov (fast-start) requires patching every offset, which is why naive concatenation breaks MP4s.',
      'M4A, M4V, MOV, HEIC, CR3: all the same box format with different brands and codecs; QuickTime invented it, MPEG standardized it.',
      'Timescales are per-track: durations are integers over a declared ticks-per-second, and 90000 is common for video for historical MPEG reasons.',
      'Fragmented MP4 (moof + mdat pairs) powers DASH and HLS-CMAF — live streams are box sequences, not one moov.',
    ],
    faq: [
      {
        q: 'Why does my MP4 need to fully download before playing?',
        a: 'Its moov box (the index) sits after mdat (the data). A progressive player can’t decode a single frame without the sample tables, so it must reach the end first. Remuxing with "fast start" (e.g. ffmpeg -movflags +faststart) moves moov to the front without re-encoding.',
      },
      {
        q: 'Are MP4 and MOV the same format?',
        a: 'Nearly: MP4 is the ISO standardization of Apple’s QuickTime container, and both are box/atom structures. Brands and allowed codecs differ (ftyp "qt  " vs "isom"/"mp42"), which is why some MOVs need remuxing to be web-safe MP4s even when the codec inside is identical.',
      },
    ],
    related: ['wav', 'icns', 'jpeg'],
  },
]
